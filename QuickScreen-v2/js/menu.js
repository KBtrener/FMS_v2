const previousShell=QSUI.shell;
QSViews.trainerAdminPanel=function(){return QSViews.adminPanel().replace('Administracja · QuickScreen','Tryb łączony · QuickScreen').replace('Panel administratora','Panel trenera + administratora').replace('Administrator','Trener + admin')};
QSUI.shell=function(content,active='clients'){
 const groups=[['Praca',[['Klienci','#/clients','Przegląd profili i historii'],['Badanie','#/assessment','Rozpocznij screening ruchowy']]],['Dashboardy',[['Dashboard klienta','#/client-panel','Postępy i wyniki klienta'],['Panel trenera','#/trainer-panel','Codzienna praca z klientami'],['Panel administratora','#/admin-panel','Zespół, role i kontrola systemu']]],['Ustawienia',[['Konfiguracja','#/configuration','Testy i reguły clearingu'],['Profil trenera','#/trainer','Dane i certyfikacje']]]];
 const current=location.hash;const preview=[['Klient','#/client-panel'],['Trener','#/trainer-panel'],['Admin','#/admin-panel'],['Trener + admin','#/trainer-admin-panel']];
 const organized=`<details class="main-menu"><summary>Menu <span aria-hidden="true">⌄</span></summary><div class="main-menu-panel"><div class="preview-switch"><span>Podgląd makiety</span><div>${preview.map(([label,route])=>`<a class="${current.includes(route.replace('#',''))?'active':''}" href="${route}">${label}</a>`).join('')}</div></div>${groups.map(([title,links])=>`<div class="main-menu-group"><span>${title}</span>${links.map(([label,route,description])=>`<a href="${route}"><b>${label}</b><small>${description}</small></a>`).join('')}</div>`).join('')}</div></details>`;
 return previousShell.call(this,content,active).replace(/<nav class="main-menu"[\s\S]*?<\/nav>/,organized);
};
